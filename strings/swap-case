import sys
raunak=raw_input()
for i in raunak:
    x=ord(i)
    if 65 <= x <= 90:
        y=i.lower()
        sys.stdout.write(y)
        
    elif 97 <= x <= 122:
        y=i.upper()
        sys.stdout.write(y)        
    else:
        sys.stdout.write(i)
